server:
  port: 8082

spring:
  kafka:
    consumer:
      bootstrap-servers: ${spring.embedded.kafka.brokers}

  main:
    banner-mode: "console"

tilsagnsbrev:
  integrasjon:
    pdfgen:
      uri: http://localhost:5913
    joark:
      uri: uri
    sts:
      uri: uri
      bruker: ${tilsagnsbrev_srvuser}
      passord: ${tilsagnsbrev_srvpwd}
    altinn:
      queue: DEV.QUEUE.1
      queueManager: QM1
      channel: DEV.ADMIN.SVRCONN
      user: ${tilsagnsbrev.mq.user}
      password: ${tilsagnsbrev.mq.pwd}
      host: localhost
      port: 32794