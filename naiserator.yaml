apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: tag-tilsagnsbrev
  namespace: default
  labels:
    team: teamtag
spec:
  image: navikt/tag-tilsagnsbrev:{{version}}
  team: teamtag
  port: 8080
  replicas:
    min: 1
    max: 1
  liveness:
    path: /tag-tilsagnsbrev/internal/healthcheck
    initialDelay: 30
  readiness:
    path: /tag-tilsagnsbrev/internal/healthcheck
    initialDelay: 30
  vault:
    enabled: true
  env:
    - name: SPRING_PROFILES_ACTIVE
      value: {{spring-profil}}
  webproxy: true
  prometheus:
    enabled: false
    path: /tag-tilsagnsbrev/internal/actuator/prometheus


